// operador de asignacion

let numeroUno = 0;
let numeroDos = 0;

//operadores aritmeticos

let suma = 5 + 8;
let resta = 5 - 8;
let multi = 5 * 8;
let division = 5 / 8;
let resto = 5 % 8;

suma += 4;
resta -= 4;
multi *= 4;
division += 4;
suma += 4;

//alert(`esto es una alaerta, el resultado es ${suma}`)
//let nombre = prompt("Dime como te llamas")
//console.log(`Mi nombre es ${nombre}`);

//simular un loging. Pedir promnt user-pass

/*
let acceso=false;

do {
    let user=prompt("Dime el nombre de usuario");
let pass=prompt("Dime el pass para acceder")
if (user=="admin" && pass=="admin") {
    alert("Loging correcto")
    acceso=true;
} else {
    alert("Loging incorrecto")
}

} while (acceso!=true);
*/

/*
let numeroUno = Number(prompt("dime el primer numero"));
let numeroDos = Number(prompt("Dime el segundo numero"));



if ((numeroUno >= 0 && !isNaN(numeroUno) )&& (numeroDos >= 0&& !isNaN(numeroDos))) {

  console.log(`La suma es:${numeroUno + numeroDos}\n`);
  console.log(`La resta es:${numeroUno - numeroDos}\n`);
  console.log(`La multi es:${numeroUno * numeroDos}\n`);
  console.log(`La division es:${numeroUno / numeroDos}\n`);
  console.log(`El resto es:${numeroUno % numeroDos}\n`);
} else {
    console.log("los numeros no son correctos");
}
*/

//operadores realcioanles

numeroUno = 6;
numeroDos = 7;

let palabraUno = "hola";
let palabraDos = "adios";

let comparacion = numeroUno >= numeroDos;

let comparacionPalabras = palabraUno == palabraDos;
